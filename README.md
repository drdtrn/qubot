Qubic Price Tracker Bot 🚀A high-performance, asynchronous Telegram bot designed to provide real-time statistics for the Qubic (QUBIC) ecosystem. Built with python-telegram-bot and aiohttp.This bot features a Smart Cache System that updates data in the background every 30 seconds. This ensures that users receive instant responses while minimizing the load on the Qubic RPC infrastructure.🛠 FeaturesInstant Responses: Served from a local cache—no waiting for API calls when you type a command.Detailed Metrics: Tracks Price (9 decimals), Market Cap, Active Addresses, and Burned $QUB$.Automated Background Refresh: The bot fetches new data independently of user requests.Safe Concurrency: Uses asyncio.Lock to prevent "Cache Stampedes" (multiple simultaneous API requests).Production Logging: Rotating logs to monitor performance and API health.🏗 System ArchitectureBackground Loop: Every 30 seconds, a task wakes up and fetches data from the Qubic RPC.Global Cache: Data is formatted and stored in memory with a timestamp.User Request: When a user triggers /dmprice, the bot simply pulls the pre-formatted text from memory.📦 Installation & Setup1. Clone the repositoryBashgit clone https://github.com/yourusername/qubic-price-bot.git
cd qubic-price-bot
2. Install DependenciesBashpip install python-telegram-bot aiohttp python-dotenv
3. Configure Environment VariablesCreate a .env file in the root directory:Code snippetBOT_TOKEN=your_telegram_bot_token_here
API_URL=https://rpc.qubic.org/v1/latest-stats
LOG_PATH=./bot.log
4. Setup .gitignoreEnsure your .gitignore contains .env to keep your token private.🚀 UsageStart the bot by running:Bashpython bot.py
Commands:/dmprice - Displays the current Qubic price, market cap, and burned supply.🛡 Security & Best PracticesSecrets Management: This bot uses python-dotenv to separate configuration from code.Asynchronous Design: Non-blocking code ensures the bot stays responsive even under high load.Error Handling: Includes try-except blocks for API failures to ensure the bot remains online even if the RPC is temporarily down.📄 RequirementsPython 3.10+python-telegram-botaiohttppython-dotenv